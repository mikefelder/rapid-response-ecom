{
  "name": "@rapid-response-shopper/api",
  "version": "1.0.0",
  "description": "REST API for product management and user preferences",
  "type": "module",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist",
    "start": "func start",
    "dev": "npm run build && func start",
    "deploy": "func azure functionapp publish"
  },
  "dependencies": {
    "@azure/cosmos": "^4.0.0",
    "@azure/identity": "^4.2.0",
    "@rapid-response-shopper/shared": "*",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@azure/functions": "^4.11.1",
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.0",
    "typescript": "^5.4.0"
  }
}
